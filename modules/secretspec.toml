[project]
name = "dotfiles"
revision = "1.0"

[providers]
keyring = "keyring://"

[profiles.default]
GITHUB_TOKEN = { description = "GitHub token used by Codex and OpenCode's GitHub MCP servers", required = true, providers = ["keyring"] }
